# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
CFLAGS="-march=native -O2 -pipe"
CXXFLAGS="${CFLAGS}"
# WARNING: Changing your CHOST is not something that should be done lightly.
# Please consult http://www.gentoo.org/doc/en/change-chost.xml before changing.
CHOST="x86_64-pc-linux-gnu"
# These are the USE and USE_EXPAND flags that were used for
# buidling in addition to wh is provided by the profile.
USE="-bindist -gallium -llvm samba cups nfs fuse vim-syntax cryptsetup \
     networkmanager evdev egl opengl lzma xinerama imagemagick \
     mtp twolame pulseaudio openssl cddb dvb lirc matroska opus sid skins \
     x264 x265 bluetooth scanner bittorrent java"
CPU_FLAGS_X86="mmx sse sse2 sse3 mmxext ssse3 sse4_1 sse4_2"
PORTDIR="/usr/portage"
DISTDIR="${PORTDIR}/distfiles"
PKGDIR="${PORTDIR}/packages"
MAKEOPTS="-j5"
CAMERAS="directory "
VIDEO_CARDS="intel fbdev vesa radeon"
INPUT_DEVICES="keyboard libinput mouse evdev joystick synaptics"
QEMU_SOFTMMU_TARGETS="arm i386 x86_64"
QEMU_USER_TARGETS="arm i386 x86_64"
PORTDIR_OVERLAY="/usr/local/portage"
